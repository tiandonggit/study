<!--components/list/list.wxml-->
<view class="item">
  <block wx:for="{{list}}" wx:for-item="item" wx:key="index">
    <view bindtap="select" data-name="{{item.name}}">{{item.name}}</view>
  </block>
</view>